description: Finish the last slice of the commit content-digest work — the member-side check is now written; validate it (build, typecheck, tests), add one remaining doc paragraph, and hand off to review.
files: packages/db-p2p/src/storage/storage-repo.ts, packages/db-p2p/src/cluster/cluster-repo.ts, packages/db-p2p/test/storage-repo.spec.ts, packages/db-p2p/test/cluster-commit-digest.spec.ts, docs/internals.md, docs/correctness.md
difficulty: medium
----

# Commit content digest — member-side check: validate + finish docs

Continuation of `commit-cert-digest-member-check` (this replaces that ticket; a prior run hit its
token budget after writing all code, tests, and the internals.md doc, but BEFORE running any
validation). **All implementation exists in the working tree — do not re-implement.** The original
ticket's full spec (checkable/abstain rule, edge cases, promise-round rationale) is reproduced in
the code's own doc comments; read those before changing behavior.

## What is already done (verify, don't redo)

- `StorageRepo.previewCommitDigest(blockId, actionId, rev)` + exported `CommitDigestPreview` type
  in `packages/db-p2p/src/storage/storage-repo.ts` (placed just above `internalCommit`; imports
  `canonicalBlockHash` from db-core). Read-only, latch-free, mirrors internalCommit's reads;
  unmaterializable base → `{ baseIndependent:false, baseRev, digest: undefined }` WITHOUT deleting
  the pending record; clones base and transform before `applyTransform`.
- `CONTENT_DIGEST_MISMATCH = 'content-digest-mismatch'` exported beside `MEMBERSHIP_NOT_ADMITTED`
  in `packages/db-p2p/src/cluster/cluster-repo.ts`; `evaluatePromise` now chains
  `validatePendOperations` → `validateCommitOperations`; `validateCommitOperations` (placed just
  above `handleCommitNeeded`) probes `previewCommitDigest` duck-typed off the `IRepo`-typed
  `storageRepo`, iterates `{ commit }` ops' `blockDigests`, ignores surplus ids, abstains on
  no-pend / digest-undefined / preview-throw / base-rev mismatch, checks always when
  `preview.baseIndependent`, rejects the whole record with `CONTENT_DIGEST_MISMATCH` on mismatch.
- Tests written:
  - `packages/db-p2p/test/storage-repo.spec.ts` — new trailing `describe('previewCommitDigest')`:
    insert/update/delete previews match what commit actually stores, no-pend → undefined,
    updates-with-no-base, wedged (unmaterializable) base preserves the pending, insert previews
    cleanly on a wedged block, single-peer fast-path commit accepts `blockDigests` (also imports
    `canonicalBlockHash` from db-core at the top).
  - `packages/db-p2p/test/cluster-commit-digest.spec.ts` — NEW file: mock-repo abstain paths
    (no capability / no digests / no pend / nothing materialized / lagging base / baseRev-omitted
    dodge / surplus id / throwing preview), checkable paths (update match+mismatch,
    base-independent ignores declared baseRev both ways), and a real-StorageRepo pend-seeded pair
    (match approves, tampered declaration rejects). Harness mirrors
    `cluster-membership-admission.spec.ts` (v1 record, two declared peers so consensus never
    executes, vote read from `record.promises[self]`, `member.dispose()` in finally).
- `docs/internals.md`: new subsection "Commit content-digest check (promise round)" under
  "Commit Path (Distributed Consensus)" — declaration, checkable/abstain table, promise-round
  placement, residual.

## Remaining TODO

- Add the correctness.md note (the only doc gap): in `docs/correctness.md` §2 Definitions, after
  the **Membership binding** paragraph (~line 56), add a short **Content digest declaration**
  definition: commit requests may carry per-block `blockDigests` declared by the transaction
  author; cohort members re-materialize from their own pended transform on the promise round and
  reject on mismatch (`content-digest-mismatch`); checkable only when base-independent (insert) or
  when the member's base rev equals the declared one — otherwise the member abstains. Then add one
  sentence to the Theorem 14 status paragraph (~line 395, "Commit signatures are not yet a trust
  anchor…") noting promise-round approvals now attest block content when checkable
  (`ClusterMember.validateCommitOperations`), while cert persistence/verification remains future
  work (`persist-block-commit-proof`).
- Validation: `yarn build && yarn typecheck && yarn test` from the root, in the foreground
  (no output redirection). Fix any fallout from this work; report genuinely pre-existing failures
  per the pre-existing-failure rules.
- Two spec assertions worth a second look if they fail (intent is right, mechanics unverified):
  - storage-repo update-preview test asserts `storedDigest(1)` still differs after committing
    rev 2 — rev 1 is reconstructed by replay even after materialization pruning, so it should
    hold, but if `getBlock(1)` surprises, drop that one assertion (the rev-2 equality is the
    load-bearing check).
  - cluster spec assumes `member.update()` on a two-peer record stops in the Promising phase
    after our single vote (superMajority = 2); if consensus unexpectedly executes, add a third
    declared peer instead of debugging the phase machine.
- Write the review-stage handoff (`tickets/review/`), delete this ticket. Handoff should carry:
  what the check does, the abstain/checkable rule, where the tests live, the single-peer fast
  path note (no consensus → no check, covered by a storage-repo test), and that
  `persist-block-commit-proof` (durable proof) is explicitly out of scope.
