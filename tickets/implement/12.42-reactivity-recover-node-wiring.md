description: Plug the already-built "what did I miss?" catch-up machinery into the live running node so a real phone or laptop can actually use it over the network, and add the real-socket test that proves it.
prereq: reactivity-notification-transport
files: packages/db-p2p/src/libp2p-node-base.ts, packages/db-p2p/src/reactivity/recover-transport.ts, packages/db-p2p/src/reactivity/subscription-manager.ts, packages/db-p2p/src/reactivity/forwarder-host.ts, packages/db-p2p/src/reactivity/protocols.ts, packages/db-p2p/src/cohort-topic/peer-sig.ts, packages/db-p2p/test/substrate-real-libp2p.integration.spec.ts, docs/reactivity.md
difficulty: medium
----

# Reactivity recover RPC — live node wiring + real-socket integration test

## STATUS — gated on 12.33; signer sub-deliverable done; node composition still deferred

> **Re-verified 2026-06-17 at HEAD `a9266ee` (this run): nothing changed — 12.33 has not landed, so no
> forward progress is possible and the ticket stays in `implement/` (deferred automatically per workflow,
> NOT blocked — the missing prereq is a tess ticket still in `implement/`; see tickets.md §Blocked L78).**
> Confirmed this run: 12.33 is still in `implement/`, still errored with a `<!-- resume-note -->`; the
> `cohortEnabled` block of `libp2p-node-base.ts` (L714-779) still installs only the cohort-topic host +
> change bridge — no `fwd`, `cohortHintCache`, `resolveCohort`, notify transport, subscriber registry, or
> production subscribe factory exists to compose into. Reimplementing 12.33 here is forbidden (combines
> tickets, gets ahead of the pipeline, collides with 12.33's in-flight resume on the *same* block); moving
> to `blocked/` is forbidden (prereq is a tess ticket in `implement/` → deferred automatically). So this run
> is a no-op on code by design: leave in place, let the runner re-pick once 12.33 lands.
>
> The **one real design point** of this ticket — the synchronous recover-request signer seam — is
> **resolved, implemented, reviewed, and complete** (HEAD `a9266ee`). It was split into its own ticket
> `reactivity-recover-request-signers`, which has since passed review and now lives at
> **`tickets/complete/12.42-reactivity-recover-request-signers.md`** (review commit `a9266ee`). That
> covered `signPeerSig` + `createRecoverRequestSigners` + `recover-signers.spec.ts` — already done, not part
> of the remaining work here. This ticket is now **only the node composition** (steps below) + the
> real-socket integration test.
>
> The node composition plugs the recover transport + handler + signers into the reactivity wiring block of
> `libp2p-node-base.ts` that `reactivity-notification-transport` (12.33) owns. **That block does not exist
> yet.** 12.33 errored mid-flight and is still in `implement/` with a resume note; the `cohortEnabled` block
> (`libp2p-node-base.ts` ~L714-779, re-confirmed this run) installs only the cohort-topic host + change
> bridge. There is no `fwd` forwarder host, no `cohortHintCache`, no `resolveCohort`/`CohortPeerResolver`,
> no notify transport, no subscriber registry, and **no production subscribe factory** anywhere in the
> codebase to compose into. (The reactivity *modules* — `notify-transport.ts`, `forwarder-host.ts`,
> `push-state-gossip.ts`, `origination-manager.ts` — exist from earlier sub-tickets, but are **not wired**
> into the node.)
>
> Per the tickets workflow this ticket is deferred automatically until 12.33's reactivity node wiring
> lands; it stays here in `implement/` and the runner re-picks it once the chain clears. Do **not**
> reimplement 12.33 inside this ticket — that would combine tickets, get ahead of the pipeline, and collide
> head-on with 12.33's in-flight resume on the **same** `cohortEnabled` block. The "design as if the prereq
> landed" rule does not apply cleanly here: 12.33 does not hand this ticket a stable *interface* to call —
> it constructs the very local variables (`fwd`, `cohortHintCache`, `resolveCohort`, the subscribe factory)
> **inside the exact block** this ticket would edit. There is no seam between the two; they share a scope.
> So the honest state is: wait for 12.33, then compose.

## Already landed (no further work — just call it)

`createRecoverRequestSigners(nodePrivateKey)` in `packages/db-p2p/src/reactivity/recover-transport.ts`
returns the `{ signBackfill, signResume }` the subscribe factory feeds the manager. The whole recover
**transport + inbound handler** (`Libp2pReactivityRecoverTransport`, `createLibp2pRecoverDialer`,
`registerRecoverHandler`, `createRecoverRequestHandler`) also already exists and is unit-tested (landed by
`reactivity-recover-rpc-transport`, 12.41, complete). This ticket only *composes* those into the node.

## Remaining work — the node composition (do once 12.33's reactivity wiring lands)

Compose with 12.33's reactivity wiring (forwarder host `fwd`, subscriber registry, notify +
push-state-gossip handlers, the shared `cohortHintCache`, and the cohort gossip transport's
`CohortPeerResolver`/FRET assembly), in the `if (cohortEnabled)` block of `libp2p-node-base.ts`:

- **Construct the dialer + transport.**
  `const recover = new Libp2pReactivityRecoverTransport({ dialer: createLibp2pRecoverDialer(node,
  protocols.recover), selfPeerId: node.peerId.toString(), cohortHintCache, resolveCohort, maxBytes })`,
  where `resolveCohort(topicId)` reuses the cohort gossip transport's FRET `CohortPeerResolver` (resolve the
  recover topic's coord → dialable **peer-id strings**, matching the notify transport's dial-target space).
- **Register the inbound handler.**
  `registerRecoverHandler(node, protocols.recover, { pushStateFor: fwd.pushStateFor.bind(fwd),
  pushStateForCollection: fwd.pushStateForCollection.bind(fwd), replayGuard:
  createCorrelationReplayGuard(), maxBytes })` — one node-level replay guard shared across recover requests.
- **Feed the subscribe factory.** The production factory that builds a `ReactivitySubscriptionManager`
  supplies, in addition to its existing options:
  - `backfillTransport: recover.backfillTransport(topicId, collectionIdB64)` and
    `resumeTransport: recover.resumeTransport(topicId, collectionIdB64)`;
  - `signBackfill` / `signResume` from **`createRecoverRequestSigners(nodePrivateKey)`** (already landed —
    no further design work, just call it);
  - `subscriberCoord`: the subscriber's **real ring coordinate** (the `participantCoord` it registers under
    at the cohort-topic tier), not the collectionId placeholder;
  - the shared `cohortHintCache` (so the transport's sticky-primary lookup and the manager's
    rotation-invalidation see the same cache).
- **Protocol registration + teardown.** `node.handle` the recover protocol with the other reactivity
  protocols; the existing `node.stop` wrapper's `node.unhandle(reactivityProtocolList(protocols))` already
  covers it (recover is in the list — confirmed in `protocols.ts`). No new timer (the replay guard is a
  plain pruned-on-access map).
- **Self-dial / co-located serve.** The transport already excludes `selfPeerId` from dial candidates
  (confirmed: `Libp2pReactivityRecoverTransport.selectTargets` skips `target === selfPeerId`), so a
  co-located subscriber falls through to other cohort members rather than dialing itself. **Recommendation:**
  accept the cohort-walk fallback (simplest, correct — any member with the gossiped `PushState` answers); an
  in-process short-circuit from the local `PushState` is a zero-RT optimization, not a correctness need.
  Decide + document either way (mirror the notify transport's self-guard).

## Real-socket integration test (env-gated)

In `substrate-real-libp2p.integration.spec.ts` (gated the same way as 12.33's notification socket-delivery
test): a remote subscriber that slept past the live tail's last delivered revision sends one `ResumeV1`
over a real socket to a real tail-cohort member and is brought current (the backfill variant) — the
recovery analogue of 12.3's notification socket-delivery test. If the gate is unset, note it is gated and
rely on the in-process `recover-transport.spec.ts` + `recover-signers.spec.ts` coverage.

## Edge cases & interactions

- **Any cohort member can serve** (ring + checkpoint are gossiped); the sticky hint is the one-RT happy
  path, the cohort-walk fallback reaches any member.
- **Sticky-hint staleness**: invalidate only on confirmed staleness (a `tail_rotated` reply / rotation
  detection — already handled by the manager), never on a transient dial failure (the transport's walk
  fallback handles that).
- **Edge node** subscribes + dials recover (never serves — no `PushState`); a recover dial to an Edge node
  finds no `PushState` and gets no reply (harmless; Edge is never a recover target in a correct walk).
- **Teardown ordering**: the recover unhandle rides the existing `node.unhandle(reactivityProtocolList)` in
  the stop wrapper, before `host.stop()`.
- **Foreign/oversized/malformed recover frame**: already bounded + no-reply in the handler; confirm the
  node-level `maxBytes` matches the notify transport's.

## TODO (remaining)

- Construct `Libp2pReactivityRecoverTransport` + `createLibp2pRecoverDialer` and `registerRecoverHandler`
  in the `cohortEnabled` reactivity block, reusing 12.33's `fwd`, `cohortHintCache`, and `CohortPeerResolver`.
- Feed `backfillTransport`/`resumeTransport`/`signBackfill`/`signResume` (from `createRecoverRequestSigners`)
  /`subscriberCoord` (real ring coord)/shared `cohortHintCache` into the production subscribe factory.
- `node.handle(protocols.recover, …)`; confirm the stop wrapper unhandles it.
- Un-skip / add the env-gated real-libp2p resume socket-delivery test.
- Update `docs/reactivity.md` §Backfill RPC / §Resume notes from "transport built" to "transport live", and
  the §Real-libp2p e2e coverage section to include the recover socket test.
- Build + test db-p2p streamed (`yarn workspace @optimystic/db-p2p test 2>&1 | tee /tmp/rx-recover-wiring.log`)
  green; run the env-gated integration spec if the gate is set, else note it is gated.
