description: A storage node decides which "ring" (how much data it holds) to sit in using a formula with no damping, so a node near a boundary can flip back and forth endlessly. Add smoothing, a dead-band, and a minimum wait between moves so the decision settles.
prereq:
files:
  - docs/arachnode-ring-handoff.md (§ Part 1 — the spec this implements)
  - packages/db-p2p/src/storage/ring-selector.ts (determineRing ~38-82, shouldTransition ~133-164, RingSelectorConfig ~6-17)
  - packages/db-p2p/src/libp2p-node-base.ts (~899-903 RingSelector construction; ~1017-1027 the 60s shouldTransition poll)
difficulty: medium
----

Implement the damping described in `docs/arachnode-ring-handoff.md` § Part 1 so the ring-depth
decision stops thrashing near a boundary. This is a self-contained change to `RingSelector` plus its
config and construction site — it does **not** touch the handoff protocol (that is the sibling
ticket `arachnode-ring-handoff-protocol`).

Today `RingSelector.determineRing()` computes `ceil(-log2(coverage))` from an **instantaneous**
`getRingStats()` + capacity snapshot (`ring-selector.ts:79`), and `shouldTransition()` returns
`determineRing() ± 1` as the target — a target recomputed from the same noisy signal on every tick.
`libp2p-node-base.ts:1017-1027` polls it every 60 s and flips the ring on any move. Near a boundary
where `-log2(coverage)` sits close to an integer, this oscillates.

## What to build

- **Smoothing (§1.1).** Keep EWMA state (α ≈ 0.2, configurable) for the demand inputs
  (`estimatedTotalData` from ring stats, and capacity), so the continuous depth
  `d = clamp(-log2(max(ε, smoothedCoverage)), 0, 16)` moves only on sustained change, not a single
  sample. State lives on the `RingSelector` instance and updates once per `shouldTransition` /
  sample tick.
- **Dead-band / hysteresis (§1.2).** For a node advertising ring `R`:
  - move **out** (`R→R+1`) only when `d ≥ R + 1 − h` **and** `usedPercent > moveOut`;
  - move **in** (`R→R-1`) only when `d ≤ R − 1 + h` **and** `usedPercent < moveIn` **and** `R > 0`.
  Default `h ≈ 0.5` so the two triggers are a full ring apart and cannot both fire.
- **Minimum dwell + single-step (§1.3).** No new move within `minDwellMs` (default 10 min) of the
  last completed move; never move while the node is already mid-move; move by exactly ±1 ring even
  when `d` implies a larger jump.
- **Config (§1.4).** Add `smoothingAlpha`, `deadband`, `minDwellMs` to `RingSelectorConfig` with the
  defaults above; wire them at the construction site in `libp2p-node-base.ts` (keep existing
  `moveOut: 0.85` / `moveIn: 0.40`). `shouldTransition` needs the node's **currently-advertised**
  ring `R` (from `fretAdapter.getMyArachnodeInfo()`), not a freshly recomputed one, as the hysteresis
  anchor.

`shouldTransition`'s target must be `R ± 1` (single step), not `determineRing() ± 1`.

## Edge cases & interactions

- **Boundary hover** — a `coverage` ratio parked so `d` sits at exactly `R` (e.g. jittering ±0.3
  around an integer) must produce **zero** moves. Test: feed an oscillating coverage sequence
  straddling a boundary and assert `shouldMove` stays false across the whole sequence.
- **Bootstrap / no ring stats** — the existing fallback (fixed `estimatedTotalData` when
  `getRingStats()` is empty) must still yield a deterministic first-boot ring; seed the EWMA from
  the first real sample rather than from 0 (a 0 seed would drag `d` for many ticks).
- **Dwell vs. genuine sustained pressure** — a node truly over capacity must still eventually move
  out after `minDwellMs`; dwell rate-limits, it does not veto. Test both: rapid flip suppressed, but
  a sustained `usedPercent > moveOut` for > `minDwellMs` does move.
- **Single-step under a two-ring gap** — if `d` implies `R+2`, one call moves to `R+1` only; assert
  the second ring is taken only on a later tick after dwell.
- **Interaction with the handoff protocol** — `shouldTransition` returning `{shouldMove:true}` is the
  *trigger*; the sibling ticket consumes it to run advertise→confirm→release. Keep the return shape
  (`{shouldMove, direction, newRingDepth}`) stable so that consumer is unaffected, or coordinate the
  shape change with that ticket.
- **`determineRing()` callers** — `createArachnodeInfo` also calls `determineRing()`; ensure the
  smoothed value it reports is consistent with the ring the node actually advertises (don't report a
  smoothed ring while advertising a different one).

## TODO

- Extend `RingSelectorConfig` with `smoothingAlpha`, `deadband`, `minDwellMs`; document defaults.
- Add EWMA state + update to `RingSelector`; derive smoothed `d`.
- Rewrite `shouldTransition` to use advertised `R`, the dead-band predicates, dwell timer, single step.
- Wire the new config at `libp2p-node-base.ts:899`.
- Unit tests: boundary-hover suppression, sustained-pressure move, single-step, dwell rate-limit,
  bootstrap seeding. (Injectable clock — do not sleep on wall time; see repo wall-clock test guidance.)
- `yarn build` + package tests green.
</content>
