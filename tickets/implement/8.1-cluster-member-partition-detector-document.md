# ClusterMember PartitionDetector — document held-but-unused dependency

description: Add a brief comment to ClusterMember's constructor explaining why `_partitionDetector` is held but not yet read, pointing to backlog ticket 6.5-partition-healing. No behavior change.
prereq: none
files:
  - packages/db-p2p/src/cluster/cluster-repo.ts (constructor parameter at line 99; ClusterMemberComponents.partitionDetector at line 44)
  - tickets/backlog/6.5-partition-healing.md (update to record where the detector should be consumed)
----

## Decision

Option A from the plan ticket: keep the parameter, document intent. The dependency is real and wired up at every call site (`libp2p-node-base.ts:293,311`, `network-manager-service.ts:73,98`, plus four test harnesses). It is `ClusterMember` itself that does not yet consume it. Option B (drop it now, re-add later) was rejected because the parameter telegraphs planned partition-healing work to readers and `tickets/backlog/6.5-partition-healing.md` explicitly names `cluster-repo.ts` as a target file — removing it would create churn across five callers for a parameter we expect to re-add.

## What changes

A single short comment near the held-but-unused parameter, plus a clarifying note in the partition-healing backlog ticket.

## TODO

- [ ] Add a one-line comment immediately above the `_partitionDetector?: PartitionDetector,` parameter at `packages/db-p2p/src/cluster/cluster-repo.ts:99` explaining it is reserved for partition-healing (backlog ticket `6.5-partition-healing`). Keep it terse — one line, no multi-paragraph block.
- [ ] Optionally add a matching one-liner above `partitionDetector?: PartitionDetector;` in the `ClusterMemberComponents` interface at `packages/db-p2p/src/cluster/cluster-repo.ts:44` so the factory signature carries the same hint. Same standard: one line.
- [ ] Update `tickets/backlog/6.5-partition-healing.md` to enumerate the three plausible consumption points inside `ClusterMember` so the future implementer doesn't have to re-derive them:
  - `processUpdate` / `handlePromiseNeeded` — reject incoming promises during a suspected partition rather than approving them, to avoid creating divergent committed state on the minority side.
  - `propagateIfNeeded` — skip propagation during partition (mirrors `block-transfer.ts:59,84`).
  - Status reporting — surface `detectPartition()` to coordinators so they can fall back to a slower / quorum-locking mode.
- [ ] Confirm build still passes (`yarn build` from the repo root or the `packages/db-p2p` package). No new tests required — there is no behavior change.
- [ ] Do not change call sites or tests. Do not rename the parameter back from `_partitionDetector` (the underscore reflects its current "intentionally unused" status and the lint rule depends on it).
