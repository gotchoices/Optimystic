description: Closing fix for forged transaction-reversals — invalidation votes are now tied to the genuinely-chosen referee set, so an attacker can't mint throwaway keys and forge a passing reversal certificate. Reviewed and accepted.
files: packages/db-core/src/log/struct.ts, packages/db-p2p/src/dispute/invalidation.ts, packages/db-p2p/src/dispute/dispute-service.ts, packages/db-p2p/src/dispute/types.ts, packages/db-p2p/src/dispute/index.ts, packages/db-p2p/src/cluster/cluster-repo.ts, packages/db-p2p/src/libp2p-node-base.ts, docs/right-is-right.md
----

# Complete: invalidation certificate — bind votes to the legitimately-selected arbitrator set (#1)

Part 2 of 2 of the forgeable-invalidation-certificate fix. The wire format was bumped to **v3** and
every counted vote is now bound to the legitimately-selected arbitrator set (`setHash`) in addition to
the reversed transaction (`targetHash`, part 1). Verification enforces this in defense-in-depth layers:
layer 1 (always — challenger set-signature + in-set membership + dedup + 2/3), layer 2 (best-effort
injected recompute that closes a fully-malicious challenger), and a degradation tier (accept-and-log
when neither recompute nor a trust anchor can anchor the set). See the implement commit `93db5cc` and
`docs/right-is-right.md` §Durable Invalidation for the full design.

## Review findings

### Verdict: **accepted.** Implementation is correct, well-layered, and faithfully matches its design and docs.

### What was checked

- **Read the implement diff first (commit `93db5cc`), fresh, before the handoff** — all 16 changed
  files: the v3 wire format (`struct.ts`), the verifier (`invalidation.ts`), origination
  (`dispute-service.ts` / `types.ts`), the cluster apply gate (`cluster-repo.ts`), the deliberately-unwired
  composition root (`libp2p-node-base.ts`), the re-exports (`index.ts`), and every touched test +
  `docs/right-is-right.md`.
- **v3 preimage non-drift (the suggested reviewer focus).** Re-derived both signed payloads by hand and
  confirmed origination and verification share the same helpers (`voteSigningPayload`,
  `arbitratorSetSigningPayload`, `computeArbitratorSetHash`), so signed and verified bytes cannot drift.
- **Adversarial probe of the layering order in `verifyInvalidationCertificate`.** Walked every
  early-return: outcome → structural presence → challenger-set signature (1a) → in-set + signature +
  dedup/equivocation count (1b) → 2/3 super-majority computed *only over in-set deduped decisive votes*
  → layer-2 recompute → degradation. No early-return skips a needed check; the super-majority is
  correctly computed after membership/dedup filtering. The set-membership check (`arbitratorSet.has`)
  precedes signature verification, and a tampered set breaks the challenger signature (1a) before any
  vote is counted. Composes correctly: layer 1b ensures votes ∈ carried set; layer 2 confirms carried
  set == genuine set ⇒ transitively votes ∈ genuine set.
- **Sybil / forge vectors.** Confirmed the closure logic: with layer 2, an attacker using the genuine
  set cannot forge genuine arbitrators' votes (no keys), and an attacker using a sybil set is rejected
  by recompute mismatch. Layer 1 alone binds the originator and defeats a third-party relay.
- **`selectArbitrators(blockId, originalPeers, …)`** excludes the original cluster (and thus the
  challenger when it is a cluster member) — matches the "next-K beyond the cluster" design.
- **Lint + build + full test suites** (see below).
- **Docs.** `docs/right-is-right.md` §Durable Invalidation rewritten to the layered model and the
  residual; verifier/struct doc-comments and the `libp2p-node-base.ts` comment cross-reference the
  trust-anchor plan ticket. Confirmed they reflect the new reality (no stale "cannot forge is partial"
  language, no stray `v2` references anywhere in `src`/`test`).

### Tests run (all green)

- `packages/db-core`: `yarn build` clean; `yarn test` → **906 passing**.
- `packages/db-p2p`: `yarn build` clean; `yarn test` → **916 passing, 30 pending, 0 failing**. The lone
  `console.error` is the cohort-topic cold-start spec deliberately exercising a parent-unreachable
  failure path (that test passes — pre-existing, unrelated to this diff).
- Root `lint` is a no-op echo (not configured); `tsc` is the type-check gate and passed for both packages.
- Re-ran the four dispute/invalidation specs after my inline fix: **84 passing**.

Test coverage is genuinely adversarial (not just happy-path): missing binding, non-set peers,
post-signing set tamper, the sybil-key headline (accepted on layer 1, rejected under recompute),
recompute genuine-vs-forged, both degradation reasons, and the no-false-positive-unanchored case — plus
the cluster-path equivalents. Edge/error/regression paths are well covered.

### Findings & disposition

- **MINOR — fixed inline.** `arbitratorSetSigningPayload` hardcoded the literal `v3set:` while
  `voteSigningPayload` derived its marker from `VOTE_VERSION`. The two preimages ship in one proof
  (a proof is wholly one wire version), so a future bump to v4 would silently leave the set-signature at
  `v3set` while votes moved to v4 — exactly the cross-version drift the implementer's "preimages cannot
  drift" intent guards against. Changed to `${VOTE_VERSION}set:` (byte-identical today: `v3set`), with a
  doc note. Fully covered by the shared-helper tests; rebuilt + re-ran (84 passing).

- **MAJOR — filed `tickets/backlog/invalidation-live-wiring-requires-arbitrator-set-anchoring.md`.**
  The accept-and-log degradation is safe *only* while invalidation is dormant (no live origination/emit
  or `onInvalidate` → `applyInvalidation` wiring). The instant invalidation is wired to a live path
  without either a validated live recompute (layer 2) or the trust-anchor chain (layer 3), the
  degradation becomes a **live forgeable-reversal vector** (the headline test confirms a forged proof is
  accepted with no recompute). This sequencing constraint previously lived only in code comments/docs and
  was not enforced by the ticket graph — the trust-anchor plan ticket does not know invalidation is a
  consumer. The new backlog ticket makes the gate first-class (prereq on
  `cohort-topic-membership-cert-trust-anchoring`) and also captures the two lower-severity residuals the
  implementer flagged (challenger ∈ cohort at layer 1; cascade children verifying at degradation
  strength). This is the disposition of the implementer's open question (gap #1): **accept the interim
  degradation and defer the live recompute**, because (a) the subsystem is dormant — no live exploit
  today, and (b) a naive live recompute risks a late-joiner liveness regression without a
  churn-tolerance window that needs live-topology validation not achievable in unit tests.

- **Honest flags #2–#5 from the handoff** (layer-3 unimplemented; challenger-anchoring depth at layer 1;
  `applyInvalidation` internal re-verify at degradation; no durable end-to-end emit wiring) were all
  verified accurate and are correctly characterized as documented residuals, not defects. They are
  rolled into the backlog ticket above where they affect the live-wiring gate; the rest are tracked by
  the existing trust-anchor plan ticket and the prior invalidation tickets' wiring-status note.

### Categories with nothing found (stated explicitly)

- **No correctness/logic defect** in the verifier layering, dedup/equivocation, super-majority math, or
  the origination↔verification preimage agreement — re-derived by hand and adversarially traced.
- **No type-safety gap.** `DisputeResolutionProof`'s three new fields are required; `buildDisputeResolutionProof`
  is the sole production constructor and threads them; all test literals were updated to v3; build is clean.
- **No resource-cleanup / DRY / modularity regression.** Shared signing helpers eliminate preimage
  duplication; the recompute capability is injected so `ClusterMember` stays network-agnostic; the
  `onUnanchored` callback is try/caught so a faulty caller cannot break verification.
- **No DoS/perf concern** introduced — verification is O(votes) with one extra set-membership check and
  at most one injected async recompute.
