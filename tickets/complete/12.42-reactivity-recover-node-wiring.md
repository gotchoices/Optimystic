description: The "what did I miss?" catch-up recovery is wired into the live running node and proven over a real network socket, so a phone or laptop that fell behind can ask a peer to bring it current. Reviewed and shipped.
files: packages/db-p2p/src/libp2p-node-base.ts, packages/db-p2p/src/reactivity/recover-transport.ts, packages/db-p2p/src/reactivity/forwarder-host.ts, packages/db-p2p/src/reactivity/protocols.ts, packages/db-p2p/test/substrate-real-libp2p.integration.spec.ts, packages/db-p2p/test/reactivity/node-wiring.spec.ts, packages/db-p2p/test/reactivity/recover-transport.spec.ts, docs/reactivity.md
----

# Reactivity recover RPC — live node wiring + real-socket integration test (COMPLETE)

The recover RPC (the pull "what did I miss?" companion to the one-way notify fan-out) is composed into the
running cohort-topic node and exercised over real libp2p sockets. The **serve side is live production wiring**:
a node answers `RecoverRequestV1` frames against its forwarder host's live `PushState`s. The **outbound
transport + request signers + shared sticky cohort-hint cache** are constructed and exposed on the node
(`reactivityRecover` / `reactivityRecoverSigners` / `reactivityCohortHintCache`) for the deferred subscribe
factory (the Quereus `Database.watch` bridge, backlog `optimystic-network-reactive-watch-integration-test`)
that will construct managers — exactly mirroring how the notify subscriber side already sits.

All implementation landed in the `if (cohortEnabled)` block of `libp2p-node-base.ts` as a "5. Recover RPC" step
after `pushStateGossip.start()` and before the teardown wrapper. Recover's protocol handler rides the existing
`node.unhandle(reactivityProtocolList(...))` teardown (recover is in that list). Docs (`docs/reactivity.md`)
moved the §Backfill RPC / §Authentication / §Real-libp2p-e2e notes from "transport built" → "node wiring live".

## Review findings

**Reviewed:** the implement diff (`9f555ff`) with fresh eyes before the handoff summary — the +61-line wiring
block, the new env-gated integration test, and the docs changes — plus the seams it calls into.

### Checked, correct (no change)
- **Coord agreement (DRY):** `resolveReactivityCohort` = `fret.assembleCohort(reactivityAddressing.coord0(topicId), cohortWantK)`
  — byte-identical to the path the push-state-gossip authenticity gate and origination gate already use, so a
  recover walk reaches exactly the cohort holding the gossiped `PushState`. No duplicated cohort logic.
- **Serve resolution:** `pushStateFor` / `pushStateForCollection` are bound to the **one** `forwarderHost`
  instance that notify fan-out + origination ingest fill; confirmed both methods exist
  (`forwarder-host.ts:229,260`) and resolve live served state (proven by the passing integration test).
- **Teardown / resource cleanup:** `recover` is in `reactivityProtocolList` (`protocols.ts:59`) so the existing
  stop-wrapper `node.unhandle(...)` releases it — no new teardown needed. The node-level `CorrelationReplayGuard`
  is a pruned-on-access map (no timer to clear). No leak.
- **maxBytes parity:** every recover seam omits `maxBytes` → `DEFAULT_STREAM_MAX_BYTES`, equal to the notify
  transport's default. No divergent literal.
- **Key agreement:** the serve handler verifies the request signature against the **dialing** peer id; the
  outbound signers sign with `nodePrivateKey` (`libp2p-node-base.ts:294`), the same key libp2p derives the
  node's peer id from — so the round trip is symmetric (proven by the integration test's real Ed25519 verify).
- **Error handling:** serve handler returns no-reply (never throws out) on decode/verify/replay/resolve failure;
  the outbound transport walks targets and throws only when all fail. Self is never dialed.
- **Docs:** read all three updated `docs/reactivity.md` sections against the code — accurate and current.

### Minor — FIXED INLINE
- **In-process coverage gap for this ticket's deliverables.** `test/reactivity/node-wiring.spec.ts` asserted
  registration/teardown of `notify` + `push-state-gossip` and the subscriber-registry exposure, but **not** the
  `recover` protocol (registered + unhandled-on-stop) nor the three node seams this ticket adds. The env-gated
  integration test exercises the serve handler but builds its *own* recover transport, so the exposed seams had
  **zero** coverage. Added assertions to all three cases (Core-enabled: `recover` registered, `reactivityRecover`
  is a `Libp2pReactivityRecoverTransport`, `reactivityRecoverSigners.{signBackfill,signResume}` present,
  `reactivityCohortHintCache.get` present, `recover` unhandled on stop; disabled: `recover` absent + all three
  seams `undefined`). All 4 node-wiring tests pass.

### Acknowledged gap — NOT a defect, no ticket filed
- The **outbound** recover transport is constructed + exposed but not yet called by any node-internal code,
  because the manager-constructing subscribe factory is the explicitly-deferred backlog item
  `optimystic-network-reactive-watch-integration-test` (the Quereus `Database.watch` → manager bridge). This is
  the same serve-live / subscribe-exposed split the notify subscriber side already has, and is the honest state
  — not a regression. When that work is scoped, advance the backlog item; do not reopen this.

### Major
- **None.** No new fix/plan/backlog tickets filed from this review.

## Validation (all green)
- `yarn workspace @optimystic/db-core build` + `yarn workspace @optimystic/db-p2p build` — clean (tsc).
- Full db-p2p suite: **804 passing / 28 pending / 0 failing** (`test/**/*.spec.ts`, dot reporter).
- node-wiring.spec.ts (with the new recover assertions): **4 passing**.
- Env-gated real-libp2p (`OPTIMYSTIC_INTEGRATION=1`): **9 passing**, including
  *"a remote subscriber resumes past the tail over a real recover socket and is brought current (backfill)"*
  (~338 ms) — production registered serve handler, real peer-key sign/verify, replay/freshness guard, end-to-end.
