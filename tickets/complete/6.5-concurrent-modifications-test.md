description: Fix collection test "should handle concurrent modifications" — already resolved
prereq: collection tests
files:
  - packages/db-core/test/collection.spec.ts
  - packages/db-core/src/collection/collection.ts
----

## Summary

The test "should handle concurrent modifications" was already fixed by commit `b2b9ce9` ("ticket(fix): concurrent-collection-creation").

### History

1. The original test used truly concurrent `Promise.all([act+sync, act+sync])` which was racy and failing.
2. Commit `44bf984` ("Punted concurrent collection bug for later") commented out the assertions.
3. Commit `b2b9ce9` replaced the single broken test with four well-structured concurrent tests:
   - `should resolve concurrent creation (first synced wins)` — basic concurrent creation
   - `should allow operations on losing collection after concurrent creation` — post-recovery usability
   - `should resolve concurrent creation with pending data on both peers` — convergence with data on both sides
   - `should handle latch-serialized concurrent sync after concurrent creation` — `Promise.all` with full convergence verification (the direct successor of the original test)

### Validation

- All 255 tests pass (`npm test`)
- Build passes cleanly (`npm run build`)
- No code changes needed — the fix was already in place
