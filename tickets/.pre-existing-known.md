# Known pre-existing failures (tess)

<!-- 2026-08-31: the distributed-diary concurrent-writes entry was REMOVED. It was not
pre-existing. Bisected to 210ebffd (per-instance Collection latch key), which unmasked a
real production race. Tracked as fix/1-bug-concurrent-create-commits-two-actions-at-one-revision. -->

- `packages/db-p2p` — `yarn workspace @optimystic/db-p2p test`, suite
  `Concurrent diary appends — acknowledged means durable`, test
  `every fulfilled append is present after convergence; losses reject as conflicts`
  (`test/concurrent-diary-append-acknowledgement.spec.ts`). DELIBERATELY RED: a new regression
  test pinning the commit-tier acknowledged-but-lost write (a commit that lost the conflict race
  is retried verbatim, reaches a second commit-consensus, every member's storage refuses it as
  stale, and the writer is still told success). Tracked as
  `implement/1-consensus-pend-refusal-commit-tier-close` — do not re-report, do not skip or
  weaken the test; it goes green when that ticket lands.
- `packages/reference-peer` — distributed-diary "should handle concurrent writes from multiple
  nodes" may still fail for the same root cause until the same ticket lands.
