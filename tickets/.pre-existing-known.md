# Known pre-existing failures (tess)

<!-- 2026-08-31: the distributed-diary concurrent-writes entry was REMOVED. It was not
pre-existing. Bisected to 210ebffd (per-instance Collection latch key), which unmasked a
real production race. Tracked as fix/1-bug-concurrent-create-commits-two-actions-at-one-revision. -->

<!-- 2026-08-31: the db-p2p concurrent-diary-append-acknowledgement.spec.ts entry was REMOVED —
the commit-tier fix landed (five arms; see implement/1-consensus-pend-refusal-commit-tier-verify3)
and the full db-p2p suite is green (2369 passing). -->

<!-- 2026-08-31: the acknowledged-but-lost arm of the reference-peer entry was RESOLVED —
the reproducer was re-run and passes in the full workspace sweep and scoped runs (3/3 writes
acknowledged AND landed). What remains below is a different, intermittent root cause. -->

- `packages/reference-peer` — distributed-diary "should handle concurrent writes from multiple
  nodes" is INTERMITTENT: it passes in normal runs but can fail with all three writes refused
  (`SyncRetryExhaustedError … stale revision … requested rev 1`, zero entries) when timing makes
  every writer lose the first-append conflict race — each writer's own half-committed first
  attempt then blocks its retries forever. Captured under heavy DEBUG logging on 2026-08-31.
  Tracked as `fix/2-all-lose-conflict-race-wedges-concurrent-first-appends` — do not re-report.
