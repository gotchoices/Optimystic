# Backlog garden report

Pass date: 2026-08-05 · Human feedback this pass: **none** — so nothing was declined. Consolidation,
metadata backfill, and ranking only.

**Queue: 46 → 39 tickets** (7 files absorbed into 6 survivors). Every ticket now carries `tradeoffs:`;
every bug and every dormant-defect ticket now carries `severity:` / `likelihood:` / `repro:`.

---

## Garden-tending log (2026-08-05, after the gardening pass above)

The pipeline had drained — `fix/`, `plan/`, `implement/`, `review/` all empty, no runner anywhere —
so under the standing "tend the garden" mandate in `AGENTS.md`, batches were promoted and a runner
(`node tess/scripts/run.mjs --refresh-index`, live strategy) was driven over them. Nothing needing a
human design decision was promoted; the four in "Decisions waiting on you" were left untouched.

**Pass 1 — the ranked top six. All six reached `complete/`** (19 live transitions, tree clean, root
`yarn lint` / `yarn build` / `yarn test` green per the review-stage reports).

| Ticket | Promoted to | Why that stage |
|---|---|---|
| `1-bug-cluster-size-resolution-single-source` | `fix/` | Bug with a verified repro; both arms need a reproduction before the invariant is designed. |
| `2-bug-optimystic-constraint-level-on-conflict-ignored` | `fix/` | Bug with a copy-paste repro. If the engine/vtab boundary turned out to be an upstream call, the fix stage would route it to `blocked/` (it did not). |
| `3-bug-orphaned-pending-after-divergent-commit` | `plan/` | Three candidate approaches in the ticket; plan resolved the design (agent-decidable, no human call). |
| `4-debt-concat-transform-overlapping-updates` | `implement/` | Fully specified: exact code site, explicit Requirements list, mirrored an already-landed sibling fix. |
| `5-debt-db-core-single-spec-import-cycle` | `fix/` | Reproduced crash, root cause not yet pinned to a fix. |
| `6-bug-docs-out-of-date-with-code` | `implement/` | Prose sweep with the file/occurrence table and scope bounds already enumerated. |

Two extra chains were filed and completed mid-run by the stages themselves
(`bug-same-key-update-reports-unique-collision-with-itself`, `oldkeyvalues-compact-tuple-hardening`),
and four new backlog tickets came out of the reviews — see *New from pass 1* below.

**Pass 2 — also drained. All six reached `complete/`** (16 live transitions, 0 agent errors, tree
clean), except that `3-debt-pending-only-insert-unreadable-with-context` correctly routed itself to
`blocked/` as `repo-pending-overlay-has-no-producer` — see *Decisions waiting on you*. Promoted after
pass 1 drained:

| Ticket | Promoted to | Why that stage |
|---|---|---|
| `1-bug-second-key-network-built-with-defaults` | `fix/` | New from pass 1, and the highest-ranked thing on the board: same defect `bug-cluster-size-resolution-single-source` just fixed inside `createLibp2pNodeBase`, still live in two composition roots. wrong-result × normal-use. |
| `2-debt-libp2p-node-base-startup-error-leak` | `fix/` | Static repro, single file, resource-cleanup fix. |
| `3-debt-pending-only-insert-unreadable-with-context` | `plan/` | Needs the "which component is authoritative" call settled first — plan resolves it and documents the tradeoff. |
| `4-debt-db-core-src-imports-own-root-barrel` | `implement/` | New from pass 1. Ten enumerated imports plus a guard spec to broaden; the follow-through on `debt-db-core-single-spec-import-cycle`, which fixed the symptom. |
| `5-debt-nominal-key-tuple-vs-row-types` | `plan/` | New from pass 1. corruption × unusual; the branded-type shape needs designing before it is written. |
| `6-feat-reference-peer-announce-addr-flag` | `plan/` | Easy feature, but the flag/README recipe boundary is worth a plan pass. |

### New from pass 1 (filed by the review stages)

| Ticket | sev × likelihood | Status |
|---|---|---|
| `bug-second-key-network-built-with-defaults` | wrong-result × normal-use | promoted, pass 2 |
| `debt-db-core-src-imports-own-root-barrel` | structural | promoted, pass 2 |
| `debt-nominal-key-tuple-vs-row-types` | corruption × unusual | promoted, pass 2 |
| `debt-doc-code-citations-rot-silently` | cosmetic | **left in backlog — your call**: the ticket's own tradeoff line offers two very different answers (build a citation checker into CI, or simply stop writing `file.ts:123` in prose). That is a cost decision, not a design detail. |

### New from pass 2 (filed by the review/plan stages)

`debt-reference-peer-cli-flags-untestable`, `debt-node-factory-wiring-steps-own-their-teardown`,
`debt-node-attachment-reads-bypass-typed-surface` — all `debt-`, none urgent, all left in `backlog/`.
Plus one new decision in `blocked/`: `repo-pending-overlay-has-no-producer`.

### Promotions stopped here — release pending

Garden tending was **halted after pass 2 at the maintainer's request**: a release is going out, so no
pass 3 was queued and `fix/`, `plan/`, `implement/`, `review/` were deliberately left empty. The
pass-3 draw below is the resume point once the release has shipped.

### Deliberately not promoted

- `debt-optimystic-session-mode-statement-savepoint-gap` — `hard`, corruption × **contrived**, and
  unreachable (no host wires session mode). Real risk to the coordinator for a dormant payoff.
- `debt-mesh-harness-arms-real-gates` — a three-arm theme whose work is integration-level; those
  specs exceed the runner's idle window, so it wants a human at the keyboard.
- The four decisions listed at the bottom of this report.

The four sub-folders (`enhancements/`, `hardening/`, `more-design/`, `speculative/`) are yours to
curate and were not touched or counted.

---

## Ranked promote-next

**Everything the original ranking named has now been promoted** — the top six drained through pass 1,
the next tier plus three pass-1 spin-offs went out in pass 2 (both tables above). What follows is the
draw for pass 3, once pass 2 lands.

| # | Ticket | sev × likelihood | Why this order |
|---|---|---|---|
| 1 | `debt-doc-code-citations-rot-silently` | cosmetic | Cheap *if* you pick "stop writing line numbers"; real machinery if you pick the checker. One sentence from you settles it and it promotes immediately. |
| 2 | `debt-mesh-harness-arms-real-gates` | test fidelity — three safety gates unproven at mesh level | The largest correctness blind spot left, but integration-level; wants a human at the keyboard rather than the runner. |
| 3 | `debt-competing-writer-test-transactor` / `debt-txn-coordinator-cache-tests` / `debt-owned-block-seed-e2e-restart-test` | test debt | Unit-level and independent of each other — a natural batch once the bug queue is empty. |
| 4 | `debt-tsconfig-exact-optional-property-types` | edge-case | Repo-wide strictness flag; mechanical but noisy, so best run when nothing else is in flight. |
| 5 | `debt-optimystic-session-mode-statement-savepoint-gap` | corruption × contrived | Deliberately last of the defects: `hard`, unreachable today, and the fix touches the coordinator's snapshot-replay. |

Below that the queue is `feat-` work and the four decisions — neither is the runner's to start.

---

## Queue overview, by cluster

### Cluster / consensus safety
| Ticket | sev × likelihood |
|---|---|
| `1-bug-cluster-size-resolution-single-source` **(theme, 2 arms)** | wrong-result × normal-use |
| `debt-mesh-harness-arms-real-gates` **(theme, 3 arms)** | test fidelity — three safety gates unproven at mesh level |
| `debt-read-repair-penalty-provable-only` | edge-case × unusual |
| `debt-read-repair-commit-cert-verification` | wrong-result × contrived |
| `debt-network-manager-coordinator-selection-is-a-stale-duplicate` | wrong-result × contrived |
| `debt-mixed-version-identify-incompatibility` | edge-case × unusual — **decision, see below** |
| `feat-admission-floor-from-observed-cohort-high-water-mark` | design not settled |
| `feat-cluster-membership-threshold-cert-anchoring` | waiting on membership anchoring |
| `feat-cohort-selection-owner-aware-placement` | replication factor promises independence it does not deliver |

### Storage / repo
| Ticket | sev × likelihood |
|---|---|
| `3-bug-orphaned-pending-after-divergent-commit` | wrong-result × unusual |
| `debt-pending-only-insert-unreadable-with-context` | wrong-result × unusual |
| `debt-libp2p-node-base-startup-error-leak` | edge-case × unusual |
| `debt-owned-block-seed-e2e-restart-test` | test debt |
| `feat-cold-range-transform-offload` | unbounded transform-log growth, long horizon |

### Transactions / OCC (db-core)
| Ticket | sev × likelihood |
|---|---|
| `4-debt-concat-transform-overlapping-updates` | corruption × contrived |
| `debt-competing-writer-test-transactor` **(theme, 2 arms)** | test debt |
| `debt-txn-coordinator-cache-tests` | test debt |
| `feat-sync-fail-fast-on-hopeless-revision` | wasted 21s retry budget on a doomed write |
| `feat-occ-priority-reservation` | design-first; Byzantine-verifiability unresolved |
| `feat-cross-collection-atomic-commit` | opt-in strong mode; gate not ticketed |
| `feat-phantom-read-protection` | **decision, see below** |

### Quereus plugin (SQL surface)
| Ticket | sev × likelihood |
|---|---|
| `2-bug-optimystic-constraint-level-on-conflict-ignored` | wrong-result × unusual |
| `debt-optimystic-session-mode-statement-savepoint-gap` | corruption × contrived |
| `debt-session-mode-bridge-coverage` **(theme, 2 arms)** | test debt |
| `debt-optimystic-key-format-migration` | corruption × unusual — **decision, see below** |
| `debt-optimystic-true-key-ordering` → `debt-optimystic-pk-range-seek` | chained perf work, gated on the migration decision |
| `feat-optimystic-legacy-commit-two-phase` | narrows a durable-split window |
| `feat-optimystic-persisted-planner-statistics` | planner runs on fixed guesses |

### Cohort-topic / matchmaking
| Ticket | sev × likelihood |
|---|---|
| `debt-cohort-topic-child-set-late-joiner-resync` | wrong-result × unusual |
| `debt-cohort-topic-pin-cohort-epoch` | edge-case × contrived |
| `debt-cohort-topic-host-eviction-teardown-test` | test debt |
| `feat-matchmaking-real-libp2p-sweep-ports` | blocked on two unwritten in-repo pieces |
| `feat-dispute-subsystem-live-activation` | gated on arbitrator-set anchoring |

### Tooling / build / platform
| Ticket | sev × likelihood |
|---|---|
| `5-debt-db-core-single-spec-import-cycle` | edge-case × normal-use |
| `6-bug-docs-out-of-date-with-code` **(theme, 2 arms)** | cosmetic × normal-use |
| `debt-ns-sqlite-concurrency-stress-test` | test debt |
| `debt-tsconfig-exact-optional-property-types` | ~211 type errors to triage |
| `feat-reference-peer-announce-addr-flag` | documented recipe not completable from the CLI |

---

## What this pass changed

### Merges (survivor ← absorbed)

- **`1-bug-cluster-size-resolution-single-source`** ← `bug-key-network-cluster-size-default-diverges`
  + `feat-warn-when-repair-floor-is-unsatisfiable`.
  One class: the node's size yardsticks are resolved in more than one place and the resolution is
  invisible. Arm A — the key network bypasses the resolution and defaults to 16 while everything else
  defaults to 10. Arm B — an embedder shipped a two-machine product whose nodes could never repair
  each other, because the resolved corroboration floor is never announced. Deliverable is the
  invariant: one resolved policy object, a construction-time coupling assertion, and a startup warning
  when the resolution is self-defeating.
- **`6-bug-docs-out-of-date-with-code`** ← `bug-docs-quote-superseded-super-majority-default`
  + `debt-docs-stale-transaction-naming`.
  One class: docs restate a value or a name owned by the source instead of citing the symbol, so they
  drift silently. Both site tables preserved verbatim.
- **`debt-mesh-harness-arms-real-gates`** ← `debt-mesh-harness-reconcile-double-bypasses-quorum`
  + `debt-mesh-level-partition-admission-regression` + `debt-mesh-client-signature-enforcement`.
  One file (`src/testing/mesh-harness.ts`), one weakness: for each safety gate the harness either
  substitutes a permissive stand-in, sets the escape hatch that disables it, or never threads the
  option that arms it. The three findings are the same bug seen three times.
- **`debt-session-mode-bridge-coverage`** ← `debt-bridge-partial-commit-branch-test`
  + `debt-session-mode-bridge-statement-recording-test`.
  Both are untestable for the same reason: no plugin-level harness stands the bridge up in session
  mode. Build the harness once; both gaps close against it.
- **`debt-competing-writer-test-transactor`** ← `debt-coordinator-retry-real-competing-committer-test`
  + `debt-e2e-stale-cache-hit-read-rejected`.
  Both need the same missing thing: a test transactor that can *durably land a competing commit
  mid-flight*. `FlakyCommitTransactor` only "stops refusing"; it never advances a log tail. Harness
  first, then the two assertions.
- **`feat-dispute-subsystem-live-activation`** ← `feat-authorize-dispute-protocol-streams` (extended,
  premise corrected). The absorbed ticket claimed any peer can already make a node vote on a dispute.
  **That is not true today** — verified: `libp2p-node-base.ts` (~643, ~1217) carries
  `[dispute-subsystem-dormant]` notes and the dispute handler is deliberately absent from the services
  map, so no peer can open the stream at all. The authorization gap becomes real *at the moment the
  activation ticket registers the handler*, which is where the decision now lives.

**Not merged, deliberately:**
- `debt-read-repair-penalty-provable-only` and `debt-read-repair-commit-cert-verification` share a
  seam (`queryClusterForLatest`) and would have made a clean theme — but **both slugs are cited from
  `NOTE:` comments in `coordinator-repo.ts`, `quorum-restore.ts`, `reconcile-block.ts`, and two spec
  files**, and retargeting those pointers is outside a gardening pass's remit. Reciprocal
  cross-references added to both bodies instead; triage them together.
- `3-bug-orphaned-pending-after-divergent-commit` and `debt-pending-only-insert-unreadable-with-context`
  are the two `KNOWN GAP:` specs in `storage-repo.spec.ts` and the same weakness seen twice, but they
  resolve in different functions with different fixes. Cross-referenced, kept separate.
- The `debt-optimystic-key-format-migration → true-key-ordering → pk-range-seek` chain is sequential
  work with distinct deliverables, already correctly chained by `prereq:`. Left alone.

### Backfills

- `tradeoffs:` added to **33** tickets (all that lacked it). Every one states a real decline argument,
  not a formality.
- `severity:` / `likelihood:` / `repro:` added to all four `bug-` tickets and to the twelve `debt-`
  tickets that describe a dormant or latent defect rather than missing coverage.
- Severity was rounded **down** in two places, with the reasoning written into the body:
  `6-bug-docs-out-of-date-with-code` is `cosmetic` (nothing misbehaves at runtime, though a reader
  mis-derives a safety margin and writes a non-compiling signature); `3-bug-orphaned-pending` is
  `wrong-result` rather than `corruption` (no data is lost — a node silently stops participating).
- `difficulty:` added where missing (9 tickets).
- **Five stale `prereq:` headers cleared** — every named ticket has already landed in `complete/`:
  `tsconfig-base-consolidation`, `cluster-membership-admission-gate`, `implement-occ-priority-aging`,
  `stale-failure-carries-coordinator-revision`, `implement-client-tx-signature-p2p` (the last carried
  by an absorbed mesh arm). A sixth, `design-cluster-membership-agreement` on
  `feat-cross-collection-atomic-commit`, names a slug that **exists nowhere on the board**; it was
  cleared and the real remaining gate (cross-collection invalidation, not ticketed anywhere) written
  into the body instead.
- Three `prereq:` headers remain and are all live: the two-step optimystic key chain, and
  `feat-dispute-subsystem-live-activation` → `backlog/hardening/invalidation-live-wiring-requires-arbitrator-set-anchoring`.

### Declines executed
None — no feedback was given this pass. No `NOTE:` comments were planted.

### Promotions executed
None during the gardening pass itself. Twelve tickets were promoted afterwards under the `AGENTS.md`
garden-tending mandate — see *Garden-tending log* above for the two batches and the stage each
landed in.

---

## Decisions waiting on you

These four are **not promotable as written** — each asks a human to choose, which the ticket rules put
in `blocked/`, not `backlog/`. A gardening pass without feedback may not move them, so they are flagged
here instead. Each is one question:

**Plus, now in `blocked/` itself** (5 files): `repo-pending-overlay-has-no-producer`, filed by pass 2's
plan stage — *`ActionContext.actionId` lets a read ask to see its own not-yet-committed change, both
storage implementations honour it, and no construction site in the repo ever sets it.* Finish the
wiring, or delete the field and the two code paths that serve it. Joins the four pre-existing blocked
tickets (`cohort-topic-admission-quorum-semantics`, `dispute-synchronous-escalation-decision`,
`matchmaking-query-rate-limit`, `st-commit-contiguity-guard-premise`).

1. **`debt-optimystic-key-format-migration`** — *Does any Optimystic data written before the
   injective-key-encoding change need to survive an in-place upgrade?* If no, close it (and consider
   stamping a format version so the next breaking change is detectable). If yes, it needs a real
   reindex capability. **This one gates two other tickets**: `debt-optimystic-true-key-ordering` and,
   behind that, `debt-optimystic-pk-range-seek`. Answering it unblocks the whole optimystic
   performance chain.
2. **`debt-mixed-version-identify-incompatibility`** — *Nodes built before and after commit `849fd94`
   can no longer complete their introduction handshake, so a staged upgrade silently splits the
   network in half.* Choose: write it down and upgrade everything at once; ship a transition release
   that speaks both strings; or (independently) make the silent skip legible in the logs.
3. **`feat-phantom-read-protection`** — *Should a transaction that reads a block which does not exist
   be invalidated if someone later creates it?* The system used to do this by accident; a recent fix
   stopped. If the answer is "no", the ticket closes having recorded that the removal was deliberate.
4. **`feat-dispute-subsystem-live-activation`** — *Should being asked to vote on a dispute require the
   same permission check as reading and writing the database?* Now embedded in the activation ticket
   (see "Who may make this node vote?"), because the gap only becomes real when the handler is
   registered. Settle it before that code is written, not after.

Two further things worth knowing:

- **One missing capability gates four backlog items.** `feat-dispute-subsystem-live-activation`,
  `feat-cluster-membership-threshold-cert-anchoring`, `debt-read-repair-commit-cert-verification`, and
  (indirectly) `debt-read-repair-penalty-provable-only` are all waiting on the same thing: the ability
  for a node to independently verify who is legitimately in a cohort, rather than re-deriving it or
  trusting first contact. That work sits in `backlog/hardening/` — chiefly
  `invalidation-live-wiring-requires-arbitrator-set-anchoring` and
  `cohort-topic-trust-anchor-txlog-committed-binding`. Promoting one of those unlocks the most
  downstream value of anything in the queue.
- **`feat-cross-collection-atomic-commit`'s real prerequisite is not ticketed.** Cross-collection
  invalidation does not exist as a ticket anywhere on the board. Whoever promotes that item must file
  it first or absorb it.
