# ClusterMember PartitionDetector — document held-but-unused dependency

description: review the documentation-only change recording why ClusterMember holds `_partitionDetector` without yet consuming it
prereq: none
files:
  - packages/db-p2p/src/cluster/cluster-repo.ts (one-line comment above `partitionDetector?:` at line 44 area; one-line comment above `_partitionDetector?:` at line 99 area)
  - tickets/backlog/6.5-partition-healing.md (added "ClusterMember integration points" section enumerating consumption points and the rename plan)
----

## What changed

Documentation-only, no behavior change:

- Added a one-line comment above `partitionDetector?: PartitionDetector;` in the `ClusterMemberComponents` interface marking it as reserved for the partition-healing consumer (backlog ticket `6.5-partition-healing`).
- Added a matching one-line comment above the `_partitionDetector?: PartitionDetector,` constructor parameter on `ClusterMember`, noting the same.
- Expanded `tickets/backlog/6.5-partition-healing.md` with a **ClusterMember integration points** section listing the three plausible consumption points (`processUpdate` / `handlePromiseNeeded`, `propagateIfNeeded`, status reporting via `detectPartition()`) and the rename-back step from `_partitionDetector` to `partitionDetector` when the future implementer wires it up.

## Decision context

Option A from the source plan ticket: keep the parameter, document intent. Option B (drop it now, re-add later) was rejected because `tickets/backlog/6.5-partition-healing.md` explicitly names `cluster-repo.ts` as a target file and the dependency is wired up at every call site (`libp2p-node-base.ts:293,311`, `network-manager-service.ts:73,98`, plus four test harnesses) — removing it would create churn across five callers for a parameter we expect to re-add.

## Review checklist

- The comments are one-liners (per ticket constraints "one line, no multi-paragraph block").
- The leading underscore on `_partitionDetector` is preserved — the lint rule for "intentionally unused" depends on it.
- No call sites were modified. No tests were added — there is no behavior change.
- `yarn workspace @optimystic/db-p2p build` passes (verified post-change, exit 0).

## Validation

- Skim `packages/db-p2p/src/cluster/cluster-repo.ts` for the two comments and confirm both reference backlog ticket `6.5-partition-healing` so a future grep finds them.
- Confirm `tickets/backlog/6.5-partition-healing.md` lists three consumption points and the rename step.
- Build: `yarn workspace @optimystic/db-p2p build` — should succeed with no new diagnostics.
- No new tests are expected; the change is comment-only.
