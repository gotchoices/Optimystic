// Fixture for test/node-builtin.test.mjs: a Node built-in module the readme's shim table has no row
// for, as a third-party package reaching `path` would (gotchoices/Optimystic#8).

import 'node:path';
