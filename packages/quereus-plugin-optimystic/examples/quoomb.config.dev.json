{
  "$schema": "https://quereus.dev/schemas/quoomb-config.json",
  "description": "Development config - uses test transactor (no network)",
  "plugins": [
    {
      "source": "${OPTIMYSTIC_PLUGIN:-@optimystic/quereus-plugin-optimystic}/plugin",
      "config": {
        "default_transactor": "test",
        "default_key_network": "test",
        "enable_cache": false
      }
    }
  ],
  "autoload": true
}

