----
description: Hello world demo app that tests across all layers
dependencies: full stack (db-core, db-p2p, UI)
----

Build a hello world demo app (e.g. messages) that exercises the full stack across all layers. This serves as both a smoke test and a reference implementation for consumers of the library.
